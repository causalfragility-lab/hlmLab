% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hlm_xint_geom.R
\name{hlm_xint_geom}
\alias{hlm_xint_geom}
\title{Geometry of a cross-level interaction (random slopes fan plot)}
\usage{
hlm_xint_geom(model, x_within, cluster, n_points = 20, n_clusters = 20)
}
\arguments{
\item{model}{A fitted \code{lmerMod} model with a random slope term
of the form \code{(x_within | cluster)}.}

\item{x_within}{Name of the Level-1 predictor with a random slope (string).}

\item{cluster}{Name of the clustering factor (string).}

\item{n_points}{Number of points to plot along the x-axis. Defaults to 20.}

\item{n_clusters}{Maximum number of clusters to display (sampled) for clarity.}
}
\value{
A ggplot object showing predicted lines by cluster.
}
\description{
Produces a "fan plot" of predicted lines for each cluster to illustrate
a cross-level interaction (random slope) in a multilevel model.
}
\examples{
\dontrun{
m <- lme4::lmer(math_score ~ SES_c + SES_mean + (SES_c | school_id), data = d)
hlm_xint_geom(m, x_within = "SES_c", cluster = "school_id")
}
}
